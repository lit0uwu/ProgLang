# Языки программирования

## Лабораторная работа 2

### Задание 1 **(2)**

**По пять любых ключевых слов из синтаксиса каждого из следующих языков:**

**С++**
- int (целое число) 
- if (условие)
- else (иначе)
- while (цикл)
- return (возврат значения)

**Java**
- public (модификатор доступа)
- class (объявление класса)
- void (тип без возвращаемого значения)
- static (статический метод/переменная)
- new (создание объекта)

**Python**
- def (создание функции)
- for (цикл)
- in (проверка вхождения / часть цикла)
- import (подключение библиотеки)
- try (начало блока обработки ошибок)

**Проверьте, можно ли создать переменные с именами записанных слов.**
- Нельзя, надо однозначно, два варианта перехода иначе будет (слово или переменная).




### Задание 2 **(2)**

**C++**

```

int main() {

int a=1, b=2;

int c=a++b;

return 0;  

}

```
Выводит - main.cpp:5:10: error: expected ‘,’ or ‘;’ before ‘b’

[int] [c] [=] [a] [++] [b] [;]

Склеивает знаки, получается a++ и отдельно b, а нужно чтобы между a и b был оператор.


**Python**

```

a=1

b=2

c=a++b

```
Выводит - пусто из исходного, если добавить print(c), то выводит - 3.

[c] [=] [a] [+] [+] [b]

Разбивает отдельно, так как в Python нет инкремента через ++ слитно, разбивает отдельно, что тоже самое что и a+(+b).




### Задание 3 **(2)**

*Рассмотрим следующую программу на языке С++.*

```

int main() {

int a=1, b=2;

int c=a+++b;

return 0; 

}

```

[int] [c] [=] [a] [++] [+] [b] [;]

Значение ```c``` выводит 3

По правилу максимального совпадения будет c = (a++) + b, инкремент постфиксный, в EAX лежит 1, получается 1 + 2

Изменить можно, вот так - ```int c = a + ++b;```
[int] [c] [=] [a] [+] [++] [b] [;]
выдаст что c теперь 4, инкремент префиксный, в EAX лежит 3, получается 1 + 3




### Задание 4 **(2)**

**Скобки являются операторами когда они выполняют вычисление**
- () как оператор - вызов функции или приведение типа.
- [] как оператор - доступ к элементу массива.

**Примеры фрагментов:**
- Является оператором: ```foo();``` - скобки оператор вызова, передают управление по адресу функции foo в памяти, после завершения - следующая инструкции программы.
- Не является оператором: ```if (x > 0)``` - просто обязательная часть разметки if, ```int x = (1 + 2) * 3;``` - разметка порядка математических операций.




### Задание 5 **(3)**

\*Рассмотрим следующую программу на языке Python.\*

```
print(*map(sum,zip(*[map(int,input().split()) for i in (1,2,3)])))
```

- **Ключевые слова:** for, in
- **Идентификаторы:** print, map, sum, zip, int, input, split, i
- **Литералы:** 1, 2, 3 (целочисленные)
- **Операторы:**
- * (распаковка)
- = (внутри генератора списков for i in...)
- **Переменные:** i
- **Функции и методы:** print, map, sum, zip, int, input, split

Функции вызываются в следующем порядке (от внутренних к внешним):
1. input() — без параметров.
2. split() — метод строки, без параметров (разбивает по пробелам).
3. map(int, ...) — параметры: тип int и результат split().
4. zip(*...) — параметры: распакованный список из 3 сгенерированных итераторов map.
5. map(sum, ...) — параметры: функция sum и объект zip.
6. print(*...) — параметры: распакованный результат верхнего map.

- **Что делает**
1. Генератор списков трижды (for i in (1, 2, 3)) запрашивает строки через input(), режет их по пробелам через split() и преобразует элементы в числа через map(int, ...). Получается список из трех итераторов.
2. zip(*...) группирует элементы с одинаковыми индексами из этих трех строк (первые элементы вместе, вторые вместе и так далее — транспонирует матрицу).
3. Внешний map(sum, ...) считает сумму каждой полученной группы (складывает элементы в каждом столбце).print(*...) распаковывает и выводит итоговые суммы через пробел.

При вводе 
1 2 3
4 5 6
7 8 9 
Выводит
12 15 18



### Задание 6 **(2)**

- // текст — однострочный комментарий.
- /* текст */ — многострочный комментарий.

```

cout<<"Hello /* Это комментарий \* world";

```
Верен, выводит Hello /* это комментарий */ world, это один строковый литералл, а не комментарий.


```

// /*

Это комментарий

*/

```
Неверен, выводит error: ‘это’ was not declared in this scope, /* не сработал т.к. в комменнатрии однострочном.


```

/* Я комментирую программы /* комментарий */ */

```
Неверен, не надо их вкладывать, один */ и так завершил бы.


```

// Я комментирую программы /* комментарий */  

```
Верен, тут только однострочный комментарий.


```

/* Я комментирую свои программы

// Это комментарий до конца строки */

*/

```
Неверен, тут только многострочный комментарий, а после еще лишняя */.


\### Задание 7 \*\*(4)\*\*

\*Найдите в стандарте С++\*



\[http://www.open-std.org/jtc1/sc22/wg21/docs/papers/2017/n4659.pdf]



\*в приложении\* \*\*A.5\*\* \*формальные синтаксические правила для 

инструкций циклов\* \*\*iteration-statement.\*\* \*Опишите на естественном языке каждую строчку

из этих правил. Для каждой строчки из правила приведите короткий пример фрагмента программы,

которая построена по этому правилу.\*



\*\*Формат ответа.\*\*



\*Как видно из правил, есть ... (указать число) инструкции циклов.

Первый вид "--- цикл ...(название), который сответсвует строке правила\*



\*... (переписать строку из правила)\*



\* Формат записи (по правилу): ключевое слово\* \*\*<<while>>\*\*, \*в круглых скобках условие\* \*\*<<condition>>\*\*,  

\*далее инструкция\* <<\*\*statement\*\*>>.



\*Пример фрагмента программы.\*

```

int x = 1;

while (x<1000) {

&#x20; cout << x;

&#x20; x \*= 2;

}

```



\### Задание 8 \*\*(3)\*\*

\*В следующей программе постройте абстрактное синтаксическое дерево для выражения,

вычисляющего значение переменной\* \*\*a\*\*.

```

int main() {

int a,x,y,z;

cin >> x >> y >> z;

a = -x+y\*(z-1);

}

```







